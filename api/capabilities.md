# Capability matrix

Generated by `heimdall contract` from each adapter's `Capabilities()` — the
same answers plan-time validation enforces. Do not edit.

| Feature | aca | cloudrun | docker | ecs | swarm |
|---|---|---|---|---|---|
| multiple services per app | full | full | full | full | full |
| sidecars in one unit | rejected — declare a sidecar as its own service | rejected — declare a sidecar as its own service | full | rejected — declare a sidecar as its own service | rejected — one container per Swarm task; declare the sidecar as its own service |
| published ports | partial — ingress exposes one target port per app; the published side of a mapping is ignored | partial — Cloud Run serves one HTTP port per service; the published side of a mapping is ignored | full | partial — awsvpc networking publishes the container port on the task interface; the host side of a mapping is ignored | full |
| more than one published port | rejected — one ingress port per Container App | rejected — one serving port per Cloud Run service | full | full | full |
| depends_on ordering | partial — apps are created in wave order; start order belongs to the platform | partial — services are created in wave order; start order belongs to the platform | full | partial — ECS orders service creation by wave; task start order belongs to the scheduler | partial — Swarm orders service creation by wave; task start order belongs to the scheduler |
| healthcheck | partial — compose healthchecks become startup probes where expressible | partial — compose healthchecks become startup probes where expressible | full | full | full |
| named volumes | rejected — named volumes need Azure Files, which is not wired yet | rejected — Cloud Run containers have no durable volumes | full | rejected — named volumes need EFS, which is not wired yet | full |
| bind mounts | rejected — there is no host to bind-mount | rejected — there is no host to bind-mount | full | rejected — there is no host to bind-mount on Fargate | rejected — a bind mount names a node-local path, which is a different file on every node; use a named volume |
| deploy.replicas | full | partial — replicas set minimum instances; Cloud Run scales above them on demand | rejected — a standalone Engine runs one container per service; use Swarm or a cloud target for replicas | full | full |
| deploy.resources | full | full | full | full | full |
| restart policy | partial — Container Apps restarts containers as part of serving | partial — Cloud Run restarts containers as part of serving; restart policies cannot be expressed | full | partial — Fargate always restarts an exited essential container; 'no' and 'on-failure' cannot be honoured | full |
| scale to zero | full | full | rejected — a standalone Engine has no scale-to-zero; use Cloud Run or Container Apps | rejected — ECS keeps desiredCount running; use Cloud Run or Container Apps | rejected — Swarm keeps replicas running; use Cloud Run or Container Apps for scale to zero |
| file secrets | rejected — Container Apps secret volumes are future work; use an environment ${secret:...} reference | rejected — Cloud Run secret volumes are future work; use an environment ${secret:...} reference | rejected — a standalone Engine has no secrets API; use Swarm, or an environment ${secret:...} reference | rejected — mount secrets through the task definition is future work; use an environment ${secret:...} reference | full |
| secret references | full | full | full | full | full |
